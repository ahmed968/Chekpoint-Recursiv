function factorial(n) {
  if (n === 0) {    //The function definition.
    return 1;   // The base condition.
  }
  return n * factorial(n - 1); //The recursive call.
}

console.log(factorial(5)); // 120
